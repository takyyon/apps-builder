import { style } from "typestyle";

export const formStyle = style({
    marginLeft: '30px',
    marginRight: '30px',
    width: '100%',
    marginBottom: '80px',
});

export const formElementStyle = style({
    width: '100%',
});

export const formLabelStyle = style({
    fontSize: '16px',
});

export const formDivStyle = style({
    marginTop: '20px',
});

export const addIconStyle = style({
    width: '200px',
    height: '200px',
    cursor: 'pointer',
});

export const chooseFileStyle = style({
    display: 'none',
});

export const buttonGroupStyle = style({
    float: 'right',
    marginLeft: 'auto',
    marginRight: '40px',
});

export const imageListDivStyle = style({
    paddingLeft: '15px',
});

export const imageListImageStyle = style({
    marginRight: '10px',
    paddingTop: '5px',
});

export const addImageStyle = style({
    width: '80px',
    height: '80px',
    cursor: 'pointer',
});

export const assignPanelIconStyle = style({
    margin: '0 auto',
});